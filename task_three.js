// FUNCTION

// Creating the multiply function that takes two numbers as parameters
function multiply(num1, num2) {
    return num1 * num2; // Multiply the numbers and return the result
  }
  
  // Calling the function and storing the returned value in a variable
  let result = multiply(5, 3);
  
  // Log the result to the console
  console.log(result); 
  